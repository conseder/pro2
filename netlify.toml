[build]
  command = "npm run build"
  publish = "dist"    # change "dist" to the correct build output (e.g. "build", "public")
  # base = ""         # optional: set if your app lives in a subdirectory

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200